import 'package:flutter/material.dart';
import 'package:my_travel/views/titleImage.dart';

class ArticlePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: titleImage(),
    );
  }
}
